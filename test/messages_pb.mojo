# Generated by protoc-gen-mojo from messages.proto. Do not edit.

"""Generated protobuf messages from `messages.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)
from proto import (
    ProtoJsonMessage,
    ProtoJsonReader,
    ProtoJsonWriter,
)


struct PayloadType(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `PayloadType`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime COMPRESSABLE: Int32 = 0
    """`COMPRESSABLE` = 0."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("COMPRESSABLE")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "COMPRESSABLE":
            return Self(value=0)
        return None


struct GrpclbRouteType(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `GrpclbRouteType`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime GRPCLB_ROUTE_TYPE_UNKNOWN: Int32 = 0
    """`GRPCLB_ROUTE_TYPE_UNKNOWN` = 0."""
    comptime GRPCLB_ROUTE_TYPE_FALLBACK: Int32 = 1
    """`GRPCLB_ROUTE_TYPE_FALLBACK` = 1."""
    comptime GRPCLB_ROUTE_TYPE_BACKEND: Int32 = 2
    """`GRPCLB_ROUTE_TYPE_BACKEND` = 2."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("GRPCLB_ROUTE_TYPE_UNKNOWN")
        elif self.value == 1:
            return String("GRPCLB_ROUTE_TYPE_FALLBACK")
        elif self.value == 2:
            return String("GRPCLB_ROUTE_TYPE_BACKEND")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "GRPCLB_ROUTE_TYPE_UNKNOWN":
            return Self(value=0)
        elif name == "GRPCLB_ROUTE_TYPE_FALLBACK":
            return Self(value=1)
        elif name == "GRPCLB_ROUTE_TYPE_BACKEND":
            return Self(value=2)
        return None


struct BoolValue(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `BoolValue` protobuf message."""

    var value: Bool
    """Field `value` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.value = False
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.value:
            writer.bool_field(1, self.value)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.value = reader.bool_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.value or writer.options.always_print_fields_with_no_presence:
            writer.field("value", "value")
            writer.bool_value(self.value)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "value" or field_name == "value":
                if seen_1:
                    raise Error("proto json: duplicate field value")
                seen_1 = True
                if reader.read_null():
                    self.value = False
                else:
                    self.value = reader.bool_value()
            else:
                reader.skip_unknown_value()


struct Payload(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `Payload` protobuf message."""

    var type: PayloadType
    """Field `type` (number 1)."""
    var body: List[Byte]
    """Field `body` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.type = PayloadType()
        self.body = List[Byte]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.type.value != 0:
            writer.int32(1, self.type.value)
        if len(self.body) != 0:
            writer.bytes_field(2, Span(self.body))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.type = PayloadType(value=reader.int32_value())
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.body = reader.bytes_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("type", "type")
            if self.type.value == 0:
                writer.string_value("COMPRESSABLE")
            else:
                writer.int32_value(self.type.value)
        if len(self.body) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("body", "body")
            writer.bytes_value(Span(self.body))
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "type" or field_name == "type":
                if seen_1:
                    raise Error("proto json: duplicate field type")
                seen_1 = True
                if reader.read_null():
                    self.type = PayloadType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "COMPRESSABLE":
                            self.type = PayloadType(value=0)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.type = PayloadType(value=reader.int32_value())
            elif field_name == "body" or field_name == "body":
                if seen_2:
                    raise Error("proto json: duplicate field body")
                seen_2 = True
                if reader.read_null():
                    self.body = List[Byte]()
                else:
                    self.body = reader.bytes_value()
            else:
                reader.skip_unknown_value()


struct EchoStatus(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `EchoStatus` protobuf message."""

    var code: Int32
    """Field `code` (number 1)."""
    var message: String
    """Field `message` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.code = 0
        self.message = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.code != 0:
            writer.int32(1, self.code)
        if self.message.byte_length() != 0:
            writer.string_field(2, self.message)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.code = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.message = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.code != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("code", "code")
            writer.int32_value(self.code)
        if self.message.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("message", "message")
            writer.string_value(self.message)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "code" or field_name == "code":
                if seen_1:
                    raise Error("proto json: duplicate field code")
                seen_1 = True
                if reader.read_null():
                    self.code = 0
                else:
                    self.code = reader.int32_value()
            elif field_name == "message" or field_name == "message":
                if seen_2:
                    raise Error("proto json: duplicate field message")
                seen_2 = True
                if reader.read_null():
                    self.message = String()
                else:
                    self.message = reader.string_value()
            else:
                reader.skip_unknown_value()


struct SimpleRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `SimpleRequest` protobuf message."""

    var response_type: PayloadType
    """Field `response_type` (number 1)."""
    var response_size: Int32
    """Field `response_size` (number 2)."""
    var payload: Optional[Payload]
    """Field `payload` (number 3)."""
    var fill_username: Bool
    """Field `fill_username` (number 4)."""
    var fill_oauth_scope: Bool
    """Field `fill_oauth_scope` (number 5)."""
    var response_compressed: Optional[BoolValue]
    """Field `response_compressed` (number 6)."""
    var response_status: Optional[EchoStatus]
    """Field `response_status` (number 7)."""
    var expect_compressed: Optional[BoolValue]
    """Field `expect_compressed` (number 8)."""
    var fill_server_id: Bool
    """Field `fill_server_id` (number 9)."""
    var fill_grpclb_route_type: Bool
    """Field `fill_grpclb_route_type` (number 10)."""
    var orca_per_query_report: Optional[TestOrcaReport]
    """Field `orca_per_query_report` (number 11)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.response_type = PayloadType()
        self.response_size = 0
        self.payload = None
        self.fill_username = False
        self.fill_oauth_scope = False
        self.response_compressed = None
        self.response_status = None
        self.expect_compressed = None
        self.fill_server_id = False
        self.fill_grpclb_route_type = False
        self.orca_per_query_report = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.response_type.value != 0:
            writer.int32(1, self.response_type.value)
        if self.response_size != 0:
            writer.int32(2, self.response_size)
        if self.payload:
            var sub = WireWriter()
            self.payload.value().encode_to(sub)
            writer.len_prefixed(3, Span(sub.buf))
        if self.fill_username:
            writer.bool_field(4, self.fill_username)
        if self.fill_oauth_scope:
            writer.bool_field(5, self.fill_oauth_scope)
        if self.response_compressed:
            var sub = WireWriter()
            self.response_compressed.value().encode_to(sub)
            writer.len_prefixed(6, Span(sub.buf))
        if self.response_status:
            var sub = WireWriter()
            self.response_status.value().encode_to(sub)
            writer.len_prefixed(7, Span(sub.buf))
        if self.expect_compressed:
            var sub = WireWriter()
            self.expect_compressed.value().encode_to(sub)
            writer.len_prefixed(8, Span(sub.buf))
        if self.fill_server_id:
            writer.bool_field(9, self.fill_server_id)
        if self.fill_grpclb_route_type:
            writer.bool_field(10, self.fill_grpclb_route_type)
        if self.orca_per_query_report:
            var sub = WireWriter()
            self.orca_per_query_report.value().encode_to(sub)
            writer.len_prefixed(11, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.response_type = PayloadType(value=reader.int32_value())
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.response_size = reader.int32_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Payload
                    if self.payload:
                        m = self.payload.take()
                    else:
                        m = Payload()
                    m.merge_from(sub)
                    self.payload = m^
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fill_username = reader.bool_value()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fill_oauth_scope = reader.bool_value()
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.response_compressed:
                        m = self.response_compressed.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.response_compressed = m^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: EchoStatus
                    if self.response_status:
                        m = self.response_status.take()
                    else:
                        m = EchoStatus()
                    m.merge_from(sub)
                    self.response_status = m^
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.expect_compressed:
                        m = self.expect_compressed.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.expect_compressed = m^
            elif field == 9:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fill_server_id = reader.bool_value()
            elif field == 10:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fill_grpclb_route_type = reader.bool_value()
            elif field == 11:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestOrcaReport
                    if self.orca_per_query_report:
                        m = self.orca_per_query_report.take()
                    else:
                        m = TestOrcaReport()
                    m.merge_from(sub)
                    self.orca_per_query_report = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.response_type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("responseType", "response_type")
            if self.response_type.value == 0:
                writer.string_value("COMPRESSABLE")
            else:
                writer.int32_value(self.response_type.value)
        if self.response_size != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("responseSize", "response_size")
            writer.int32_value(self.response_size)
        if self.payload:
            writer.field("payload", "payload")
            writer.message_value(self.payload.value())
        if self.fill_username or writer.options.always_print_fields_with_no_presence:
            writer.field("fillUsername", "fill_username")
            writer.bool_value(self.fill_username)
        if self.fill_oauth_scope or writer.options.always_print_fields_with_no_presence:
            writer.field("fillOauthScope", "fill_oauth_scope")
            writer.bool_value(self.fill_oauth_scope)
        if self.response_compressed:
            writer.field("responseCompressed", "response_compressed")
            writer.message_value(self.response_compressed.value())
        if self.response_status:
            writer.field("responseStatus", "response_status")
            writer.message_value(self.response_status.value())
        if self.expect_compressed:
            writer.field("expectCompressed", "expect_compressed")
            writer.message_value(self.expect_compressed.value())
        if self.fill_server_id or writer.options.always_print_fields_with_no_presence:
            writer.field("fillServerId", "fill_server_id")
            writer.bool_value(self.fill_server_id)
        if self.fill_grpclb_route_type or writer.options.always_print_fields_with_no_presence:
            writer.field("fillGrpclbRouteType", "fill_grpclb_route_type")
            writer.bool_value(self.fill_grpclb_route_type)
        if self.orca_per_query_report:
            writer.field("orcaPerQueryReport", "orca_per_query_report")
            writer.message_value(self.orca_per_query_report.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "responseType" or field_name == "response_type":
                if seen_1:
                    raise Error("proto json: duplicate field responseType")
                seen_1 = True
                if reader.read_null():
                    self.response_type = PayloadType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "COMPRESSABLE":
                            self.response_type = PayloadType(value=0)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.response_type = PayloadType(value=reader.int32_value())
            elif field_name == "responseSize" or field_name == "response_size":
                if seen_2:
                    raise Error("proto json: duplicate field responseSize")
                seen_2 = True
                if reader.read_null():
                    self.response_size = 0
                else:
                    self.response_size = reader.int32_value()
            elif field_name == "payload" or field_name == "payload":
                if seen_3:
                    raise Error("proto json: duplicate field payload")
                seen_3 = True
                if reader.read_null():
                    self.payload = None
                else:
                    self.payload = reader.message_value[Payload]()
            elif field_name == "fillUsername" or field_name == "fill_username":
                if seen_4:
                    raise Error("proto json: duplicate field fillUsername")
                seen_4 = True
                if reader.read_null():
                    self.fill_username = False
                else:
                    self.fill_username = reader.bool_value()
            elif field_name == "fillOauthScope" or field_name == "fill_oauth_scope":
                if seen_5:
                    raise Error("proto json: duplicate field fillOauthScope")
                seen_5 = True
                if reader.read_null():
                    self.fill_oauth_scope = False
                else:
                    self.fill_oauth_scope = reader.bool_value()
            elif field_name == "responseCompressed" or field_name == "response_compressed":
                if seen_6:
                    raise Error("proto json: duplicate field responseCompressed")
                seen_6 = True
                if reader.read_null():
                    self.response_compressed = None
                else:
                    self.response_compressed = reader.message_value[BoolValue]()
            elif field_name == "responseStatus" or field_name == "response_status":
                if seen_7:
                    raise Error("proto json: duplicate field responseStatus")
                seen_7 = True
                if reader.read_null():
                    self.response_status = None
                else:
                    self.response_status = reader.message_value[EchoStatus]()
            elif field_name == "expectCompressed" or field_name == "expect_compressed":
                if seen_8:
                    raise Error("proto json: duplicate field expectCompressed")
                seen_8 = True
                if reader.read_null():
                    self.expect_compressed = None
                else:
                    self.expect_compressed = reader.message_value[BoolValue]()
            elif field_name == "fillServerId" or field_name == "fill_server_id":
                if seen_9:
                    raise Error("proto json: duplicate field fillServerId")
                seen_9 = True
                if reader.read_null():
                    self.fill_server_id = False
                else:
                    self.fill_server_id = reader.bool_value()
            elif field_name == "fillGrpclbRouteType" or field_name == "fill_grpclb_route_type":
                if seen_10:
                    raise Error("proto json: duplicate field fillGrpclbRouteType")
                seen_10 = True
                if reader.read_null():
                    self.fill_grpclb_route_type = False
                else:
                    self.fill_grpclb_route_type = reader.bool_value()
            elif field_name == "orcaPerQueryReport" or field_name == "orca_per_query_report":
                if seen_11:
                    raise Error("proto json: duplicate field orcaPerQueryReport")
                seen_11 = True
                if reader.read_null():
                    self.orca_per_query_report = None
                else:
                    self.orca_per_query_report = reader.message_value[TestOrcaReport]()
            else:
                reader.skip_unknown_value()


struct SimpleResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `SimpleResponse` protobuf message."""

    var payload: Optional[Payload]
    """Field `payload` (number 1)."""
    var username: String
    """Field `username` (number 2)."""
    var oauth_scope: String
    """Field `oauth_scope` (number 3)."""
    var server_id: String
    """Field `server_id` (number 4)."""
    var grpclb_route_type: GrpclbRouteType
    """Field `grpclb_route_type` (number 5)."""
    var hostname: String
    """Field `hostname` (number 6)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.payload = None
        self.username = String()
        self.oauth_scope = String()
        self.server_id = String()
        self.grpclb_route_type = GrpclbRouteType()
        self.hostname = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.payload:
            var sub = WireWriter()
            self.payload.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.username.byte_length() != 0:
            writer.string_field(2, self.username)
        if self.oauth_scope.byte_length() != 0:
            writer.string_field(3, self.oauth_scope)
        if self.server_id.byte_length() != 0:
            writer.string_field(4, self.server_id)
        if self.grpclb_route_type.value != 0:
            writer.int32(5, self.grpclb_route_type.value)
        if self.hostname.byte_length() != 0:
            writer.string_field(6, self.hostname)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Payload
                    if self.payload:
                        m = self.payload.take()
                    else:
                        m = Payload()
                    m.merge_from(sub)
                    self.payload = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.username = reader.string_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oauth_scope = reader.string_value()
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.server_id = reader.string_value()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.grpclb_route_type = GrpclbRouteType(value=reader.int32_value())
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.hostname = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.payload:
            writer.field("payload", "payload")
            writer.message_value(self.payload.value())
        if self.username.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("username", "username")
            writer.string_value(self.username)
        if self.oauth_scope.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("oauthScope", "oauth_scope")
            writer.string_value(self.oauth_scope)
        if self.server_id.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("serverId", "server_id")
            writer.string_value(self.server_id)
        if self.grpclb_route_type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("grpclbRouteType", "grpclb_route_type")
            if self.grpclb_route_type.value == 0:
                writer.string_value("GRPCLB_ROUTE_TYPE_UNKNOWN")
            elif self.grpclb_route_type.value == 1:
                writer.string_value("GRPCLB_ROUTE_TYPE_FALLBACK")
            elif self.grpclb_route_type.value == 2:
                writer.string_value("GRPCLB_ROUTE_TYPE_BACKEND")
            else:
                writer.int32_value(self.grpclb_route_type.value)
        if self.hostname.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("hostname", "hostname")
            writer.string_value(self.hostname)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "payload" or field_name == "payload":
                if seen_1:
                    raise Error("proto json: duplicate field payload")
                seen_1 = True
                if reader.read_null():
                    self.payload = None
                else:
                    self.payload = reader.message_value[Payload]()
            elif field_name == "username" or field_name == "username":
                if seen_2:
                    raise Error("proto json: duplicate field username")
                seen_2 = True
                if reader.read_null():
                    self.username = String()
                else:
                    self.username = reader.string_value()
            elif field_name == "oauthScope" or field_name == "oauth_scope":
                if seen_3:
                    raise Error("proto json: duplicate field oauthScope")
                seen_3 = True
                if reader.read_null():
                    self.oauth_scope = String()
                else:
                    self.oauth_scope = reader.string_value()
            elif field_name == "serverId" or field_name == "server_id":
                if seen_4:
                    raise Error("proto json: duplicate field serverId")
                seen_4 = True
                if reader.read_null():
                    self.server_id = String()
                else:
                    self.server_id = reader.string_value()
            elif field_name == "grpclbRouteType" or field_name == "grpclb_route_type":
                if seen_5:
                    raise Error("proto json: duplicate field grpclbRouteType")
                seen_5 = True
                if reader.read_null():
                    self.grpclb_route_type = GrpclbRouteType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "GRPCLB_ROUTE_TYPE_UNKNOWN":
                            self.grpclb_route_type = GrpclbRouteType(value=0)
                        elif enum_name.value() == "GRPCLB_ROUTE_TYPE_FALLBACK":
                            self.grpclb_route_type = GrpclbRouteType(value=1)
                        elif enum_name.value() == "GRPCLB_ROUTE_TYPE_BACKEND":
                            self.grpclb_route_type = GrpclbRouteType(value=2)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.grpclb_route_type = GrpclbRouteType(value=reader.int32_value())
            elif field_name == "hostname" or field_name == "hostname":
                if seen_6:
                    raise Error("proto json: duplicate field hostname")
                seen_6 = True
                if reader.read_null():
                    self.hostname = String()
                else:
                    self.hostname = reader.string_value()
            else:
                reader.skip_unknown_value()


struct StreamingInputCallRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `StreamingInputCallRequest` protobuf message."""

    var payload: Optional[Payload]
    """Field `payload` (number 1)."""
    var expect_compressed: Optional[BoolValue]
    """Field `expect_compressed` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.payload = None
        self.expect_compressed = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.payload:
            var sub = WireWriter()
            self.payload.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.expect_compressed:
            var sub = WireWriter()
            self.expect_compressed.value().encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Payload
                    if self.payload:
                        m = self.payload.take()
                    else:
                        m = Payload()
                    m.merge_from(sub)
                    self.payload = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.expect_compressed:
                        m = self.expect_compressed.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.expect_compressed = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.payload:
            writer.field("payload", "payload")
            writer.message_value(self.payload.value())
        if self.expect_compressed:
            writer.field("expectCompressed", "expect_compressed")
            writer.message_value(self.expect_compressed.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "payload" or field_name == "payload":
                if seen_1:
                    raise Error("proto json: duplicate field payload")
                seen_1 = True
                if reader.read_null():
                    self.payload = None
                else:
                    self.payload = reader.message_value[Payload]()
            elif field_name == "expectCompressed" or field_name == "expect_compressed":
                if seen_2:
                    raise Error("proto json: duplicate field expectCompressed")
                seen_2 = True
                if reader.read_null():
                    self.expect_compressed = None
                else:
                    self.expect_compressed = reader.message_value[BoolValue]()
            else:
                reader.skip_unknown_value()


struct StreamingInputCallResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `StreamingInputCallResponse` protobuf message."""

    var aggregated_payload_size: Int32
    """Field `aggregated_payload_size` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.aggregated_payload_size = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.aggregated_payload_size != 0:
            writer.int32(1, self.aggregated_payload_size)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.aggregated_payload_size = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.aggregated_payload_size != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("aggregatedPayloadSize", "aggregated_payload_size")
            writer.int32_value(self.aggregated_payload_size)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "aggregatedPayloadSize" or field_name == "aggregated_payload_size":
                if seen_1:
                    raise Error("proto json: duplicate field aggregatedPayloadSize")
                seen_1 = True
                if reader.read_null():
                    self.aggregated_payload_size = 0
                else:
                    self.aggregated_payload_size = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct ResponseParameters(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ResponseParameters` protobuf message."""

    var size: Int32
    """Field `size` (number 1)."""
    var interval_us: Int32
    """Field `interval_us` (number 2)."""
    var compressed: Optional[BoolValue]
    """Field `compressed` (number 3)."""
    var fill_peer_socket_address: Optional[BoolValue]
    """Field `fill_peer_socket_address` (number 4)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.size = 0
        self.interval_us = 0
        self.compressed = None
        self.fill_peer_socket_address = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.size != 0:
            writer.int32(1, self.size)
        if self.interval_us != 0:
            writer.int32(2, self.interval_us)
        if self.compressed:
            var sub = WireWriter()
            self.compressed.value().encode_to(sub)
            writer.len_prefixed(3, Span(sub.buf))
        if self.fill_peer_socket_address:
            var sub = WireWriter()
            self.fill_peer_socket_address.value().encode_to(sub)
            writer.len_prefixed(4, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.size = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.interval_us = reader.int32_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.compressed:
                        m = self.compressed.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.compressed = m^
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.fill_peer_socket_address:
                        m = self.fill_peer_socket_address.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.fill_peer_socket_address = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.size != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("size", "size")
            writer.int32_value(self.size)
        if self.interval_us != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("intervalUs", "interval_us")
            writer.int32_value(self.interval_us)
        if self.compressed:
            writer.field("compressed", "compressed")
            writer.message_value(self.compressed.value())
        if self.fill_peer_socket_address:
            writer.field("fillPeerSocketAddress", "fill_peer_socket_address")
            writer.message_value(self.fill_peer_socket_address.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "size" or field_name == "size":
                if seen_1:
                    raise Error("proto json: duplicate field size")
                seen_1 = True
                if reader.read_null():
                    self.size = 0
                else:
                    self.size = reader.int32_value()
            elif field_name == "intervalUs" or field_name == "interval_us":
                if seen_2:
                    raise Error("proto json: duplicate field intervalUs")
                seen_2 = True
                if reader.read_null():
                    self.interval_us = 0
                else:
                    self.interval_us = reader.int32_value()
            elif field_name == "compressed" or field_name == "compressed":
                if seen_3:
                    raise Error("proto json: duplicate field compressed")
                seen_3 = True
                if reader.read_null():
                    self.compressed = None
                else:
                    self.compressed = reader.message_value[BoolValue]()
            elif field_name == "fillPeerSocketAddress" or field_name == "fill_peer_socket_address":
                if seen_4:
                    raise Error("proto json: duplicate field fillPeerSocketAddress")
                seen_4 = True
                if reader.read_null():
                    self.fill_peer_socket_address = None
                else:
                    self.fill_peer_socket_address = reader.message_value[BoolValue]()
            else:
                reader.skip_unknown_value()


struct StreamingOutputCallRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `StreamingOutputCallRequest` protobuf message."""

    var response_type: PayloadType
    """Field `response_type` (number 1)."""
    var response_parameters: List[ResponseParameters]
    """Field `response_parameters` (number 2, repeated)."""
    var payload: Optional[Payload]
    """Field `payload` (number 3)."""
    var response_status: Optional[EchoStatus]
    """Field `response_status` (number 7)."""
    var orca_oob_report: Optional[TestOrcaReport]
    """Field `orca_oob_report` (number 8)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.response_type = PayloadType()
        self.response_parameters = List[ResponseParameters]()
        self.payload = None
        self.response_status = None
        self.orca_oob_report = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.response_type.value != 0:
            writer.int32(1, self.response_type.value)
        for v in self.response_parameters:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        if self.payload:
            var sub = WireWriter()
            self.payload.value().encode_to(sub)
            writer.len_prefixed(3, Span(sub.buf))
        if self.response_status:
            var sub = WireWriter()
            self.response_status.value().encode_to(sub)
            writer.len_prefixed(7, Span(sub.buf))
        if self.orca_oob_report:
            var sub = WireWriter()
            self.orca_oob_report.value().encode_to(sub)
            writer.len_prefixed(8, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.response_type = PayloadType(value=reader.int32_value())
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ResponseParameters()
                    m.merge_from(sub)
                    self.response_parameters.append(m^)
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Payload
                    if self.payload:
                        m = self.payload.take()
                    else:
                        m = Payload()
                    m.merge_from(sub)
                    self.payload = m^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: EchoStatus
                    if self.response_status:
                        m = self.response_status.take()
                    else:
                        m = EchoStatus()
                    m.merge_from(sub)
                    self.response_status = m^
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestOrcaReport
                    if self.orca_oob_report:
                        m = self.orca_oob_report.take()
                    else:
                        m = TestOrcaReport()
                    m.merge_from(sub)
                    self.orca_oob_report = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.response_type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("responseType", "response_type")
            if self.response_type.value == 0:
                writer.string_value("COMPRESSABLE")
            else:
                writer.int32_value(self.response_type.value)
        if len(self.response_parameters) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("responseParameters", "response_parameters")
            writer.begin_array()
            for item in self.response_parameters:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if self.payload:
            writer.field("payload", "payload")
            writer.message_value(self.payload.value())
        if self.response_status:
            writer.field("responseStatus", "response_status")
            writer.message_value(self.response_status.value())
        if self.orca_oob_report:
            writer.field("orcaOobReport", "orca_oob_report")
            writer.message_value(self.orca_oob_report.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_7 = False
        var seen_8 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "responseType" or field_name == "response_type":
                if seen_1:
                    raise Error("proto json: duplicate field responseType")
                seen_1 = True
                if reader.read_null():
                    self.response_type = PayloadType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "COMPRESSABLE":
                            self.response_type = PayloadType(value=0)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.response_type = PayloadType(value=reader.int32_value())
            elif field_name == "responseParameters" or field_name == "response_parameters":
                if seen_2:
                    raise Error("proto json: duplicate field responseParameters")
                seen_2 = True
                if reader.read_null():
                    self.response_parameters = List[ResponseParameters]()
                else:
                    self.response_parameters = List[ResponseParameters]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.response_parameters.append(reader.message_value[ResponseParameters]())
            elif field_name == "payload" or field_name == "payload":
                if seen_3:
                    raise Error("proto json: duplicate field payload")
                seen_3 = True
                if reader.read_null():
                    self.payload = None
                else:
                    self.payload = reader.message_value[Payload]()
            elif field_name == "responseStatus" or field_name == "response_status":
                if seen_7:
                    raise Error("proto json: duplicate field responseStatus")
                seen_7 = True
                if reader.read_null():
                    self.response_status = None
                else:
                    self.response_status = reader.message_value[EchoStatus]()
            elif field_name == "orcaOobReport" or field_name == "orca_oob_report":
                if seen_8:
                    raise Error("proto json: duplicate field orcaOobReport")
                seen_8 = True
                if reader.read_null():
                    self.orca_oob_report = None
                else:
                    self.orca_oob_report = reader.message_value[TestOrcaReport]()
            else:
                reader.skip_unknown_value()


struct StreamingOutputCallResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `StreamingOutputCallResponse` protobuf message."""

    var payload: Optional[Payload]
    """Field `payload` (number 1)."""
    var peer_socket_address: String
    """Field `peer_socket_address` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.payload = None
        self.peer_socket_address = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.payload:
            var sub = WireWriter()
            self.payload.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.peer_socket_address.byte_length() != 0:
            writer.string_field(2, self.peer_socket_address)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Payload
                    if self.payload:
                        m = self.payload.take()
                    else:
                        m = Payload()
                    m.merge_from(sub)
                    self.payload = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.peer_socket_address = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.payload:
            writer.field("payload", "payload")
            writer.message_value(self.payload.value())
        if self.peer_socket_address.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("peerSocketAddress", "peer_socket_address")
            writer.string_value(self.peer_socket_address)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "payload" or field_name == "payload":
                if seen_1:
                    raise Error("proto json: duplicate field payload")
                seen_1 = True
                if reader.read_null():
                    self.payload = None
                else:
                    self.payload = reader.message_value[Payload]()
            elif field_name == "peerSocketAddress" or field_name == "peer_socket_address":
                if seen_2:
                    raise Error("proto json: duplicate field peerSocketAddress")
                seen_2 = True
                if reader.read_null():
                    self.peer_socket_address = String()
                else:
                    self.peer_socket_address = reader.string_value()
            else:
                reader.skip_unknown_value()


struct ReconnectParams(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ReconnectParams` protobuf message."""

    var max_reconnect_backoff_ms: Int32
    """Field `max_reconnect_backoff_ms` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.max_reconnect_backoff_ms = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.max_reconnect_backoff_ms != 0:
            writer.int32(1, self.max_reconnect_backoff_ms)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.max_reconnect_backoff_ms = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.max_reconnect_backoff_ms != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("maxReconnectBackoffMs", "max_reconnect_backoff_ms")
            writer.int32_value(self.max_reconnect_backoff_ms)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "maxReconnectBackoffMs" or field_name == "max_reconnect_backoff_ms":
                if seen_1:
                    raise Error("proto json: duplicate field maxReconnectBackoffMs")
                seen_1 = True
                if reader.read_null():
                    self.max_reconnect_backoff_ms = 0
                else:
                    self.max_reconnect_backoff_ms = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct ReconnectInfo(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ReconnectInfo` protobuf message."""

    var passed: Bool
    """Field `passed` (number 1)."""
    var backoff_ms: List[Int32]
    """Field `backoff_ms` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.passed = False
        self.backoff_ms = List[Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.passed:
            writer.bool_field(1, self.passed)
        if len(self.backoff_ms) != 0:
            var sub = WireWriter()
            for v in self.backoff_ms:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.passed = reader.bool_value()
            elif field == 2:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.backoff_ms.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.backoff_ms.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.passed or writer.options.always_print_fields_with_no_presence:
            writer.field("passed", "passed")
            writer.bool_value(self.passed)
        if len(self.backoff_ms) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("backoffMs", "backoff_ms")
            writer.begin_array()
            for item in self.backoff_ms:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "passed" or field_name == "passed":
                if seen_1:
                    raise Error("proto json: duplicate field passed")
                seen_1 = True
                if reader.read_null():
                    self.passed = False
                else:
                    self.passed = reader.bool_value()
            elif field_name == "backoffMs" or field_name == "backoff_ms":
                if seen_2:
                    raise Error("proto json: duplicate field backoffMs")
                seen_2 = True
                if reader.read_null():
                    self.backoff_ms = List[Int32]()
                else:
                    self.backoff_ms = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.backoff_ms.append(reader.int32_value())
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `LoadBalancerStatsRequest` protobuf message."""

    var num_rpcs: Int32
    """Field `num_rpcs` (number 1)."""
    var timeout_sec: Int32
    """Field `timeout_sec` (number 2)."""
    var metadata_keys: List[String]
    """Field `metadata_keys` (number 3, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.num_rpcs = 0
        self.timeout_sec = 0
        self.metadata_keys = List[String]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.num_rpcs != 0:
            writer.int32(1, self.num_rpcs)
        if self.timeout_sec != 0:
            writer.int32(2, self.timeout_sec)
        for v in self.metadata_keys:
            writer.string_field(3, v)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.num_rpcs = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.timeout_sec = reader.int32_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.metadata_keys.append(reader.string_value())
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.num_rpcs != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("numRpcs", "num_rpcs")
            writer.int32_value(self.num_rpcs)
        if self.timeout_sec != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("timeoutSec", "timeout_sec")
            writer.int32_value(self.timeout_sec)
        if len(self.metadata_keys) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("metadataKeys", "metadata_keys")
            writer.begin_array()
            for item in self.metadata_keys:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "numRpcs" or field_name == "num_rpcs":
                if seen_1:
                    raise Error("proto json: duplicate field numRpcs")
                seen_1 = True
                if reader.read_null():
                    self.num_rpcs = 0
                else:
                    self.num_rpcs = reader.int32_value()
            elif field_name == "timeoutSec" or field_name == "timeout_sec":
                if seen_2:
                    raise Error("proto json: duplicate field timeoutSec")
                seen_2 = True
                if reader.read_null():
                    self.timeout_sec = 0
                else:
                    self.timeout_sec = reader.int32_value()
            elif field_name == "metadataKeys" or field_name == "metadata_keys":
                if seen_3:
                    raise Error("proto json: duplicate field metadataKeys")
                seen_3 = True
                if reader.read_null():
                    self.metadata_keys = List[String]()
                else:
                    self.metadata_keys = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.metadata_keys.append(reader.string_value())
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsResponse_MetadataType(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `MetadataType`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime UNKNOWN: Int32 = 0
    """`UNKNOWN` = 0."""
    comptime INITIAL: Int32 = 1
    """`INITIAL` = 1."""
    comptime TRAILING: Int32 = 2
    """`TRAILING` = 2."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("UNKNOWN")
        elif self.value == 1:
            return String("INITIAL")
        elif self.value == 2:
            return String("TRAILING")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "UNKNOWN":
            return Self(value=0)
        elif name == "INITIAL":
            return Self(value=1)
        elif name == "TRAILING":
            return Self(value=2)
        return None


struct LoadBalancerStatsResponse_MetadataEntry(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `MetadataEntry` protobuf message."""

    var key: String
    """Field `key` (number 1)."""
    var value: String
    """Field `value` (number 2)."""
    var type: LoadBalancerStatsResponse_MetadataType
    """Field `type` (number 3)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.key = String()
        self.value = String()
        self.type = LoadBalancerStatsResponse_MetadataType()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.key.byte_length() != 0:
            writer.string_field(1, self.key)
        if self.value.byte_length() != 0:
            writer.string_field(2, self.value)
        if self.type.value != 0:
            writer.int32(3, self.type.value)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.key = reader.string_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.value = reader.string_value()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.type = LoadBalancerStatsResponse_MetadataType(value=reader.int32_value())
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.key.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("key", "key")
            writer.string_value(self.key)
        if self.value.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("value", "value")
            writer.string_value(self.value)
        if self.type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("type", "type")
            if self.type.value == 0:
                writer.string_value("UNKNOWN")
            elif self.type.value == 1:
                writer.string_value("INITIAL")
            elif self.type.value == 2:
                writer.string_value("TRAILING")
            else:
                writer.int32_value(self.type.value)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "key" or field_name == "key":
                if seen_1:
                    raise Error("proto json: duplicate field key")
                seen_1 = True
                if reader.read_null():
                    self.key = String()
                else:
                    self.key = reader.string_value()
            elif field_name == "value" or field_name == "value":
                if seen_2:
                    raise Error("proto json: duplicate field value")
                seen_2 = True
                if reader.read_null():
                    self.value = String()
                else:
                    self.value = reader.string_value()
            elif field_name == "type" or field_name == "type":
                if seen_3:
                    raise Error("proto json: duplicate field type")
                seen_3 = True
                if reader.read_null():
                    self.type = LoadBalancerStatsResponse_MetadataType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "UNKNOWN":
                            self.type = LoadBalancerStatsResponse_MetadataType(value=0)
                        elif enum_name.value() == "INITIAL":
                            self.type = LoadBalancerStatsResponse_MetadataType(value=1)
                        elif enum_name.value() == "TRAILING":
                            self.type = LoadBalancerStatsResponse_MetadataType(value=2)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.type = LoadBalancerStatsResponse_MetadataType(value=reader.int32_value())
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsResponse_RpcMetadata(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `RpcMetadata` protobuf message."""

    var metadata: List[LoadBalancerStatsResponse_MetadataEntry]
    """Field `metadata` (number 1, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.metadata = List[LoadBalancerStatsResponse_MetadataEntry]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for v in self.metadata:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = LoadBalancerStatsResponse_MetadataEntry()
                    m.merge_from(sub)
                    self.metadata.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.metadata) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("metadata", "metadata")
            writer.begin_array()
            for item in self.metadata:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "metadata" or field_name == "metadata":
                if seen_1:
                    raise Error("proto json: duplicate field metadata")
                seen_1 = True
                if reader.read_null():
                    self.metadata = List[LoadBalancerStatsResponse_MetadataEntry]()
                else:
                    self.metadata = List[LoadBalancerStatsResponse_MetadataEntry]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.metadata.append(reader.message_value[LoadBalancerStatsResponse_MetadataEntry]())
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsResponse_MetadataByPeer(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `MetadataByPeer` protobuf message."""

    var rpc_metadata: List[LoadBalancerStatsResponse_RpcMetadata]
    """Field `rpc_metadata` (number 1, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.rpc_metadata = List[LoadBalancerStatsResponse_RpcMetadata]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for v in self.rpc_metadata:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = LoadBalancerStatsResponse_RpcMetadata()
                    m.merge_from(sub)
                    self.rpc_metadata.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.rpc_metadata) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rpcMetadata", "rpc_metadata")
            writer.begin_array()
            for item in self.rpc_metadata:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "rpcMetadata" or field_name == "rpc_metadata":
                if seen_1:
                    raise Error("proto json: duplicate field rpcMetadata")
                seen_1 = True
                if reader.read_null():
                    self.rpc_metadata = List[LoadBalancerStatsResponse_RpcMetadata]()
                else:
                    self.rpc_metadata = List[LoadBalancerStatsResponse_RpcMetadata]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.rpc_metadata.append(reader.message_value[LoadBalancerStatsResponse_RpcMetadata]())
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsResponse_RpcsByPeer(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `RpcsByPeer` protobuf message."""

    var rpcs_by_peer: Dict[String, Int32]
    """Field `rpcs_by_peer` (number 1, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.rpcs_by_peer = Dict[String, Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.rpcs_by_peer.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(1, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.rpcs_by_peer[key^] = value
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.rpcs_by_peer) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rpcsByPeer", "rpcs_by_peer")
            writer.begin_map()
            for entry in self.rpcs_by_peer.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "rpcsByPeer" or field_name == "rpcs_by_peer":
                if seen_1:
                    raise Error("proto json: duplicate field rpcsByPeer")
                seen_1 = True
                if reader.read_null():
                    self.rpcs_by_peer = Dict[String, Int32]()
                else:
                    self.rpcs_by_peer = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.rpcs_by_peer[map_key^] = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct LoadBalancerStatsResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `LoadBalancerStatsResponse` protobuf message."""

    var rpcs_by_peer: Dict[String, Int32]
    """Field `rpcs_by_peer` (number 1, map field)."""
    var num_failures: Int32
    """Field `num_failures` (number 2)."""
    var rpcs_by_method: Dict[String, LoadBalancerStatsResponse_RpcsByPeer]
    """Field `rpcs_by_method` (number 3, map field)."""
    var metadatas_by_peer: Dict[String, LoadBalancerStatsResponse_MetadataByPeer]
    """Field `metadatas_by_peer` (number 4, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.rpcs_by_peer = Dict[String, Int32]()
        self.num_failures = 0
        self.rpcs_by_method = Dict[String, LoadBalancerStatsResponse_RpcsByPeer]()
        self.metadatas_by_peer = Dict[String, LoadBalancerStatsResponse_MetadataByPeer]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.rpcs_by_peer.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(1, Span(sub.buf))
        if self.num_failures != 0:
            writer.int32(2, self.num_failures)
        for entry in self.rpcs_by_method.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(3, Span(sub.buf))
        for entry in self.metadatas_by_peer.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(4, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.rpcs_by_peer[key^] = value
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.num_failures = reader.int32_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: LoadBalancerStatsResponse_RpcsByPeer = LoadBalancerStatsResponse_RpcsByPeer()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.rpcs_by_method[key^] = value^
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: LoadBalancerStatsResponse_MetadataByPeer = LoadBalancerStatsResponse_MetadataByPeer()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.metadatas_by_peer[key^] = value^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.rpcs_by_peer) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rpcsByPeer", "rpcs_by_peer")
            writer.begin_map()
            for entry in self.rpcs_by_peer.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if self.num_failures != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("numFailures", "num_failures")
            writer.int32_value(self.num_failures)
        if len(self.rpcs_by_method) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rpcsByMethod", "rpcs_by_method")
            writer.begin_map()
            for entry in self.rpcs_by_method.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        if len(self.metadatas_by_peer) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("metadatasByPeer", "metadatas_by_peer")
            writer.begin_map()
            for entry in self.metadatas_by_peer.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "rpcsByPeer" or field_name == "rpcs_by_peer":
                if seen_1:
                    raise Error("proto json: duplicate field rpcsByPeer")
                seen_1 = True
                if reader.read_null():
                    self.rpcs_by_peer = Dict[String, Int32]()
                else:
                    self.rpcs_by_peer = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.rpcs_by_peer[map_key^] = reader.int32_value()
            elif field_name == "numFailures" or field_name == "num_failures":
                if seen_2:
                    raise Error("proto json: duplicate field numFailures")
                seen_2 = True
                if reader.read_null():
                    self.num_failures = 0
                else:
                    self.num_failures = reader.int32_value()
            elif field_name == "rpcsByMethod" or field_name == "rpcs_by_method":
                if seen_3:
                    raise Error("proto json: duplicate field rpcsByMethod")
                seen_3 = True
                if reader.read_null():
                    self.rpcs_by_method = Dict[String, LoadBalancerStatsResponse_RpcsByPeer]()
                else:
                    self.rpcs_by_method = Dict[String, LoadBalancerStatsResponse_RpcsByPeer]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.rpcs_by_method[map_key^] = reader.message_value[LoadBalancerStatsResponse_RpcsByPeer]()
            elif field_name == "metadatasByPeer" or field_name == "metadatas_by_peer":
                if seen_4:
                    raise Error("proto json: duplicate field metadatasByPeer")
                seen_4 = True
                if reader.read_null():
                    self.metadatas_by_peer = Dict[String, LoadBalancerStatsResponse_MetadataByPeer]()
                else:
                    self.metadatas_by_peer = Dict[String, LoadBalancerStatsResponse_MetadataByPeer]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.metadatas_by_peer[map_key^] = reader.message_value[LoadBalancerStatsResponse_MetadataByPeer]()
            else:
                reader.skip_unknown_value()


struct LoadBalancerAccumulatedStatsRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `LoadBalancerAccumulatedStatsRequest` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            reader.skip_unknown_value()


struct LoadBalancerAccumulatedStatsResponse_MethodStats(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `MethodStats` protobuf message."""

    var rpcs_started: Int32
    """Field `rpcs_started` (number 1)."""
    var result: Dict[Int32, Int32]
    """Field `result` (number 2, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.rpcs_started = 0
        self.result = Dict[Int32, Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.rpcs_started != 0:
            writer.int32(1, self.rpcs_started)
        for entry in self.result.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.rpcs_started = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.result[key] = value
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.rpcs_started != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rpcsStarted", "rpcs_started")
            writer.int32_value(self.rpcs_started)
        if len(self.result) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("result", "result")
            writer.begin_map()
            for entry in self.result.items():
                writer.map_key(String(entry.key))
                writer.int32_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "rpcsStarted" or field_name == "rpcs_started":
                if seen_1:
                    raise Error("proto json: duplicate field rpcsStarted")
                seen_1 = True
                if reader.read_null():
                    self.rpcs_started = 0
                else:
                    self.rpcs_started = reader.int32_value()
            elif field_name == "result" or field_name == "result":
                if seen_2:
                    raise Error("proto json: duplicate field result")
                seen_2 = True
                if reader.read_null():
                    self.result = Dict[Int32, Int32]()
                else:
                    self.result = Dict[Int32, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.result[parsed_map_key] = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct LoadBalancerAccumulatedStatsResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `LoadBalancerAccumulatedStatsResponse` protobuf message."""

    var num_rpcs_started_by_method: Dict[String, Int32]
    """Field `num_rpcs_started_by_method` (number 1, map field)."""
    var num_rpcs_succeeded_by_method: Dict[String, Int32]
    """Field `num_rpcs_succeeded_by_method` (number 2, map field)."""
    var num_rpcs_failed_by_method: Dict[String, Int32]
    """Field `num_rpcs_failed_by_method` (number 3, map field)."""
    var stats_per_method: Dict[String, LoadBalancerAccumulatedStatsResponse_MethodStats]
    """Field `stats_per_method` (number 4, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.num_rpcs_started_by_method = Dict[String, Int32]()
        self.num_rpcs_succeeded_by_method = Dict[String, Int32]()
        self.num_rpcs_failed_by_method = Dict[String, Int32]()
        self.stats_per_method = Dict[String, LoadBalancerAccumulatedStatsResponse_MethodStats]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.num_rpcs_started_by_method.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(1, Span(sub.buf))
        for entry in self.num_rpcs_succeeded_by_method.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(2, Span(sub.buf))
        for entry in self.num_rpcs_failed_by_method.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(3, Span(sub.buf))
        for entry in self.stats_per_method.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(4, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.num_rpcs_started_by_method[key^] = value
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.num_rpcs_succeeded_by_method[key^] = value
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.num_rpcs_failed_by_method[key^] = value
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: LoadBalancerAccumulatedStatsResponse_MethodStats = LoadBalancerAccumulatedStatsResponse_MethodStats()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.stats_per_method[key^] = value^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.num_rpcs_started_by_method) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("numRpcsStartedByMethod", "num_rpcs_started_by_method")
            writer.begin_map()
            for entry in self.num_rpcs_started_by_method.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.num_rpcs_succeeded_by_method) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("numRpcsSucceededByMethod", "num_rpcs_succeeded_by_method")
            writer.begin_map()
            for entry in self.num_rpcs_succeeded_by_method.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.num_rpcs_failed_by_method) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("numRpcsFailedByMethod", "num_rpcs_failed_by_method")
            writer.begin_map()
            for entry in self.num_rpcs_failed_by_method.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.stats_per_method) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("statsPerMethod", "stats_per_method")
            writer.begin_map()
            for entry in self.stats_per_method.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "numRpcsStartedByMethod" or field_name == "num_rpcs_started_by_method":
                if seen_1:
                    raise Error("proto json: duplicate field numRpcsStartedByMethod")
                seen_1 = True
                if reader.read_null():
                    self.num_rpcs_started_by_method = Dict[String, Int32]()
                else:
                    self.num_rpcs_started_by_method = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.num_rpcs_started_by_method[map_key^] = reader.int32_value()
            elif field_name == "numRpcsSucceededByMethod" or field_name == "num_rpcs_succeeded_by_method":
                if seen_2:
                    raise Error("proto json: duplicate field numRpcsSucceededByMethod")
                seen_2 = True
                if reader.read_null():
                    self.num_rpcs_succeeded_by_method = Dict[String, Int32]()
                else:
                    self.num_rpcs_succeeded_by_method = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.num_rpcs_succeeded_by_method[map_key^] = reader.int32_value()
            elif field_name == "numRpcsFailedByMethod" or field_name == "num_rpcs_failed_by_method":
                if seen_3:
                    raise Error("proto json: duplicate field numRpcsFailedByMethod")
                seen_3 = True
                if reader.read_null():
                    self.num_rpcs_failed_by_method = Dict[String, Int32]()
                else:
                    self.num_rpcs_failed_by_method = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.num_rpcs_failed_by_method[map_key^] = reader.int32_value()
            elif field_name == "statsPerMethod" or field_name == "stats_per_method":
                if seen_4:
                    raise Error("proto json: duplicate field statsPerMethod")
                seen_4 = True
                if reader.read_null():
                    self.stats_per_method = Dict[String, LoadBalancerAccumulatedStatsResponse_MethodStats]()
                else:
                    self.stats_per_method = Dict[String, LoadBalancerAccumulatedStatsResponse_MethodStats]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.stats_per_method[map_key^] = reader.message_value[LoadBalancerAccumulatedStatsResponse_MethodStats]()
            else:
                reader.skip_unknown_value()


struct ClientConfigureRequest_RpcType(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `RpcType`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime EMPTY_CALL: Int32 = 0
    """`EMPTY_CALL` = 0."""
    comptime UNARY_CALL: Int32 = 1
    """`UNARY_CALL` = 1."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("EMPTY_CALL")
        elif self.value == 1:
            return String("UNARY_CALL")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "EMPTY_CALL":
            return Self(value=0)
        elif name == "UNARY_CALL":
            return Self(value=1)
        return None


struct ClientConfigureRequest_Metadata(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `Metadata` protobuf message."""

    var type: ClientConfigureRequest_RpcType
    """Field `type` (number 1)."""
    var key: String
    """Field `key` (number 2)."""
    var value: String
    """Field `value` (number 3)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.type = ClientConfigureRequest_RpcType()
        self.key = String()
        self.value = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.type.value != 0:
            writer.int32(1, self.type.value)
        if self.key.byte_length() != 0:
            writer.string_field(2, self.key)
        if self.value.byte_length() != 0:
            writer.string_field(3, self.value)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.type = ClientConfigureRequest_RpcType(value=reader.int32_value())
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.key = reader.string_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.value = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.type.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("type", "type")
            if self.type.value == 0:
                writer.string_value("EMPTY_CALL")
            elif self.type.value == 1:
                writer.string_value("UNARY_CALL")
            else:
                writer.int32_value(self.type.value)
        if self.key.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("key", "key")
            writer.string_value(self.key)
        if self.value.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("value", "value")
            writer.string_value(self.value)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "type" or field_name == "type":
                if seen_1:
                    raise Error("proto json: duplicate field type")
                seen_1 = True
                if reader.read_null():
                    self.type = ClientConfigureRequest_RpcType()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "EMPTY_CALL":
                            self.type = ClientConfigureRequest_RpcType(value=0)
                        elif enum_name.value() == "UNARY_CALL":
                            self.type = ClientConfigureRequest_RpcType(value=1)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.type = ClientConfigureRequest_RpcType(value=reader.int32_value())
            elif field_name == "key" or field_name == "key":
                if seen_2:
                    raise Error("proto json: duplicate field key")
                seen_2 = True
                if reader.read_null():
                    self.key = String()
                else:
                    self.key = reader.string_value()
            elif field_name == "value" or field_name == "value":
                if seen_3:
                    raise Error("proto json: duplicate field value")
                seen_3 = True
                if reader.read_null():
                    self.value = String()
                else:
                    self.value = reader.string_value()
            else:
                reader.skip_unknown_value()


struct ClientConfigureRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ClientConfigureRequest` protobuf message."""

    var types: List[ClientConfigureRequest_RpcType]
    """Field `types` (number 1, repeated)."""
    var metadata: List[ClientConfigureRequest_Metadata]
    """Field `metadata` (number 2, repeated)."""
    var timeout_sec: Int32
    """Field `timeout_sec` (number 3)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.types = List[ClientConfigureRequest_RpcType]()
        self.metadata = List[ClientConfigureRequest_Metadata]()
        self.timeout_sec = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if len(self.types) != 0:
            var sub = WireWriter()
            for v in self.types:
                sub.varint(UInt64(Int64(v.value)))
            writer.len_prefixed(1, Span(sub.buf))
        for v in self.metadata:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        if self.timeout_sec != 0:
            writer.int32(3, self.timeout_sec)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.types.append(ClientConfigureRequest_RpcType(value=sub.int32_value()))
                elif wire_type == WIRE_VARINT:
                    self.types.append(ClientConfigureRequest_RpcType(value=reader.int32_value()))
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ClientConfigureRequest_Metadata()
                    m.merge_from(sub)
                    self.metadata.append(m^)
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.timeout_sec = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.types) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("types", "types")
            writer.begin_array()
            for item in self.types:
                writer.array_item()
                if item.value == 0:
                    writer.string_value("EMPTY_CALL")
                elif item.value == 1:
                    writer.string_value("UNARY_CALL")
                else:
                    writer.int32_value(item.value)
            writer.end_array()
        if len(self.metadata) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("metadata", "metadata")
            writer.begin_array()
            for item in self.metadata:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if self.timeout_sec != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("timeoutSec", "timeout_sec")
            writer.int32_value(self.timeout_sec)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "types" or field_name == "types":
                if seen_1:
                    raise Error("proto json: duplicate field types")
                seen_1 = True
                if reader.read_null():
                    self.types = List[ClientConfigureRequest_RpcType]()
                else:
                    self.types = List[ClientConfigureRequest_RpcType]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "EMPTY_CALL":
                                self.types.append(ClientConfigureRequest_RpcType(value=0))
                            elif enum_name.value() == "UNARY_CALL":
                                self.types.append(ClientConfigureRequest_RpcType(value=1))
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.types.append(ClientConfigureRequest_RpcType(value=reader.int32_value()))
            elif field_name == "metadata" or field_name == "metadata":
                if seen_2:
                    raise Error("proto json: duplicate field metadata")
                seen_2 = True
                if reader.read_null():
                    self.metadata = List[ClientConfigureRequest_Metadata]()
                else:
                    self.metadata = List[ClientConfigureRequest_Metadata]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.metadata.append(reader.message_value[ClientConfigureRequest_Metadata]())
            elif field_name == "timeoutSec" or field_name == "timeout_sec":
                if seen_3:
                    raise Error("proto json: duplicate field timeoutSec")
                seen_3 = True
                if reader.read_null():
                    self.timeout_sec = 0
                else:
                    self.timeout_sec = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct ClientConfigureResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ClientConfigureResponse` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            reader.skip_unknown_value()


struct MemorySize(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `MemorySize` protobuf message."""

    var rss: Int64
    """Field `rss` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.rss = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.rss != 0:
            writer.int64(1, self.rss)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.rss = reader.int64_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.rss != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("rss", "rss")
            writer.int64_value(self.rss)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "rss" or field_name == "rss":
                if seen_1:
                    raise Error("proto json: duplicate field rss")
                seen_1 = True
                if reader.read_null():
                    self.rss = 0
                else:
                    self.rss = reader.int64_value()
            else:
                reader.skip_unknown_value()


struct TestOrcaReport(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `TestOrcaReport` protobuf message."""

    var cpu_utilization: Float64
    """Field `cpu_utilization` (number 1)."""
    var memory_utilization: Float64
    """Field `memory_utilization` (number 2)."""
    var request_cost: Dict[String, Float64]
    """Field `request_cost` (number 3, map field)."""
    var utilization: Dict[String, Float64]
    """Field `utilization` (number 4, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.cpu_utilization = 0.0
        self.memory_utilization = 0.0
        self.request_cost = Dict[String, Float64]()
        self.utilization = Dict[String, Float64]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if UInt64(self.cpu_utilization.to_bits()) != 0:
            writer.double_field(1, self.cpu_utilization)
        if UInt64(self.memory_utilization.to_bits()) != 0:
            writer.double_field(2, self.memory_utilization)
        for entry in self.request_cost.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if UInt64(entry.value.to_bits()) != 0:
                sub.double_field(2, entry.value)
            writer.len_prefixed(3, Span(sub.buf))
        for entry in self.utilization.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if UInt64(entry.value.to_bits()) != 0:
                sub.double_field(2, entry.value)
            writer.len_prefixed(4, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.cpu_utilization = reader.double_value()
            elif field == 2:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.memory_utilization = reader.double_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Float64 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.double_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.request_cost[key^] = value
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Float64 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.double_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.utilization[key^] = value
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if UInt64(self.cpu_utilization.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("cpuUtilization", "cpu_utilization")
            writer.float64_value(self.cpu_utilization)
        if UInt64(self.memory_utilization.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("memoryUtilization", "memory_utilization")
            writer.float64_value(self.memory_utilization)
        if len(self.request_cost) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("requestCost", "request_cost")
            writer.begin_map()
            for entry in self.request_cost.items():
                writer.map_key(entry.key)
                writer.float64_value(entry.value)
            writer.end_map()
        if len(self.utilization) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("utilization", "utilization")
            writer.begin_map()
            for entry in self.utilization.items():
                writer.map_key(entry.key)
                writer.float64_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "cpuUtilization" or field_name == "cpu_utilization":
                if seen_1:
                    raise Error("proto json: duplicate field cpuUtilization")
                seen_1 = True
                if reader.read_null():
                    self.cpu_utilization = 0.0
                else:
                    self.cpu_utilization = reader.float64_value()
            elif field_name == "memoryUtilization" or field_name == "memory_utilization":
                if seen_2:
                    raise Error("proto json: duplicate field memoryUtilization")
                seen_2 = True
                if reader.read_null():
                    self.memory_utilization = 0.0
                else:
                    self.memory_utilization = reader.float64_value()
            elif field_name == "requestCost" or field_name == "request_cost":
                if seen_3:
                    raise Error("proto json: duplicate field requestCost")
                seen_3 = True
                if reader.read_null():
                    self.request_cost = Dict[String, Float64]()
                else:
                    self.request_cost = Dict[String, Float64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.request_cost[map_key^] = reader.float64_value()
            elif field_name == "utilization" or field_name == "utilization":
                if seen_4:
                    raise Error("proto json: duplicate field utilization")
                seen_4 = True
                if reader.read_null():
                    self.utilization = Dict[String, Float64]()
                else:
                    self.utilization = Dict[String, Float64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.utilization[map_key^] = reader.float64_value()
            else:
                reader.skip_unknown_value()


struct SetReturnStatusRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `SetReturnStatusRequest` protobuf message."""

    var grpc_code_to_return: Int32
    """Field `grpc_code_to_return` (number 1)."""
    var grpc_status_description: String
    """Field `grpc_status_description` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.grpc_code_to_return = 0
        self.grpc_status_description = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.grpc_code_to_return != 0:
            writer.int32(1, self.grpc_code_to_return)
        if self.grpc_status_description.byte_length() != 0:
            writer.string_field(2, self.grpc_status_description)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.grpc_code_to_return = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.grpc_status_description = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.grpc_code_to_return != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("grpcCodeToReturn", "grpc_code_to_return")
            writer.int32_value(self.grpc_code_to_return)
        if self.grpc_status_description.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("grpcStatusDescription", "grpc_status_description")
            writer.string_value(self.grpc_status_description)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "grpcCodeToReturn" or field_name == "grpc_code_to_return":
                if seen_1:
                    raise Error("proto json: duplicate field grpcCodeToReturn")
                seen_1 = True
                if reader.read_null():
                    self.grpc_code_to_return = 0
                else:
                    self.grpc_code_to_return = reader.int32_value()
            elif field_name == "grpcStatusDescription" or field_name == "grpc_status_description":
                if seen_2:
                    raise Error("proto json: duplicate field grpcStatusDescription")
                seen_2 = True
                if reader.read_null():
                    self.grpc_status_description = String()
                else:
                    self.grpc_status_description = reader.string_value()
            else:
                reader.skip_unknown_value()


struct HookRequest_HookRequestCommand(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `HookRequestCommand`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime UNSPECIFIED: Int32 = 0
    """`UNSPECIFIED` = 0."""
    comptime START: Int32 = 1
    """`START` = 1."""
    comptime STOP: Int32 = 2
    """`STOP` = 2."""
    comptime RETURN: Int32 = 3
    """`RETURN` = 3."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("UNSPECIFIED")
        elif self.value == 1:
            return String("START")
        elif self.value == 2:
            return String("STOP")
        elif self.value == 3:
            return String("RETURN")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "UNSPECIFIED":
            return Self(value=0)
        elif name == "START":
            return Self(value=1)
        elif name == "STOP":
            return Self(value=2)
        elif name == "RETURN":
            return Self(value=3)
        return None


struct HookRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `HookRequest` protobuf message."""

    var command: HookRequest_HookRequestCommand
    """Field `command` (number 1)."""
    var grpc_code_to_return: Int32
    """Field `grpc_code_to_return` (number 2)."""
    var grpc_status_description: String
    """Field `grpc_status_description` (number 3)."""
    var server_port: Int32
    """Field `server_port` (number 4)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.command = HookRequest_HookRequestCommand()
        self.grpc_code_to_return = 0
        self.grpc_status_description = String()
        self.server_port = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.command.value != 0:
            writer.int32(1, self.command.value)
        if self.grpc_code_to_return != 0:
            writer.int32(2, self.grpc_code_to_return)
        if self.grpc_status_description.byte_length() != 0:
            writer.string_field(3, self.grpc_status_description)
        if self.server_port != 0:
            writer.int32(4, self.server_port)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.command = HookRequest_HookRequestCommand(value=reader.int32_value())
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.grpc_code_to_return = reader.int32_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.grpc_status_description = reader.string_value()
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.server_port = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.command.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("command", "command")
            if self.command.value == 0:
                writer.string_value("UNSPECIFIED")
            elif self.command.value == 1:
                writer.string_value("START")
            elif self.command.value == 2:
                writer.string_value("STOP")
            elif self.command.value == 3:
                writer.string_value("RETURN")
            else:
                writer.int32_value(self.command.value)
        if self.grpc_code_to_return != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("grpcCodeToReturn", "grpc_code_to_return")
            writer.int32_value(self.grpc_code_to_return)
        if self.grpc_status_description.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("grpcStatusDescription", "grpc_status_description")
            writer.string_value(self.grpc_status_description)
        if self.server_port != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("serverPort", "server_port")
            writer.int32_value(self.server_port)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "command" or field_name == "command":
                if seen_1:
                    raise Error("proto json: duplicate field command")
                seen_1 = True
                if reader.read_null():
                    self.command = HookRequest_HookRequestCommand()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "UNSPECIFIED":
                            self.command = HookRequest_HookRequestCommand(value=0)
                        elif enum_name.value() == "START":
                            self.command = HookRequest_HookRequestCommand(value=1)
                        elif enum_name.value() == "STOP":
                            self.command = HookRequest_HookRequestCommand(value=2)
                        elif enum_name.value() == "RETURN":
                            self.command = HookRequest_HookRequestCommand(value=3)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.command = HookRequest_HookRequestCommand(value=reader.int32_value())
            elif field_name == "grpcCodeToReturn" or field_name == "grpc_code_to_return":
                if seen_2:
                    raise Error("proto json: duplicate field grpcCodeToReturn")
                seen_2 = True
                if reader.read_null():
                    self.grpc_code_to_return = 0
                else:
                    self.grpc_code_to_return = reader.int32_value()
            elif field_name == "grpcStatusDescription" or field_name == "grpc_status_description":
                if seen_3:
                    raise Error("proto json: duplicate field grpcStatusDescription")
                seen_3 = True
                if reader.read_null():
                    self.grpc_status_description = String()
                else:
                    self.grpc_status_description = reader.string_value()
            elif field_name == "serverPort" or field_name == "server_port":
                if seen_4:
                    raise Error("proto json: duplicate field serverPort")
                seen_4 = True
                if reader.read_null():
                    self.server_port = 0
                else:
                    self.server_port = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct HookResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `HookResponse` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            reader.skip_unknown_value()
